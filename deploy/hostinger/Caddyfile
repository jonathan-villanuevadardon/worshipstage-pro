{$DOMAIN} {
	encode zstd gzip

	handle_path /hcgi/api/* {
		reverse_proxy api:3001
	}

	handle {
		root * /srv
		try_files {path} /index.html

		header /index.html Cache-Control "no-cache, no-store, must-revalidate"
		header /assets/* Cache-Control "public, max-age=31536000, immutable"

		file_server
	}
}
